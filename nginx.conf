daemon off;

events {}

http {
    server {
        listen 80;
        server_name localhost;
        location / {
            root /srv/static;
            index index.html;
        }
    }

    server {
        listen 80;
        server_name 0.0.0.0;

        location / {
            proxy_pass         http://localhost:9999;
        }
    }

    server {
        listen 80 default_server;
        return 404 "404";
    }
}